apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: userapp-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: userapp.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: userapp-clusterip
                port:
                  number: 80